---
id: BE-07
title: Dashboard reads RPMP synced tables
status: blocked
domain: backend
blocked_by:
  - BE-05
  - BE-04
fr:
  - FR-2.2
  - FR-2.3
  - FR-2.6
---

# BE-07: Dashboard reads RPMP synced tables

`GET` summary, execution-trend, and errors use the **same JSON** as BE-02 and BE-04. Implementation reads RPMP `use_cases` / `executions` / `execution_errors` when rows exist. File stub remains for tests that do not set a populated DB.

Freshness: prefer latest successful `sync_runs.finished_at`. If never synced, keep stub behavior for test, or `freshness.status` that FE can show as never_synced. Empty production-like DB: do not invent KPIs.

## In scope

- Repo aggregate queries (explicit columns, period `[from, to)` like BE-02).
- Wire `cmd/api` dashboard usecases to repo, not to the file stub, when configured (e.g. always repo in API process; tests may still inject stub).
- Map zero-volume success_rate `null` as today.
- `503` only when source/sync policy says fail-closed (document in NOTES). Prefer last good + failed freshness if that is simpler and matches Decision 8.

## Out of scope

- cmd/sync (BE-06). Changing frozen JSON field names. Frontend. New vendor fields in JSON.

## Frozen contract

Identical to BE-02 summary and BE-04 trend/errors JSON.

## Acceptance

- [ ] After a successful sync (or test seed of RPMP tables), summary/trend/errors match grouping rules, not JobState.
- [ ] HTTP JSON unchanged.
- [ ] Tests without source still pass (stub or seeded RPMP).
- [ ] No frontend edits.

## Execution prompt

```text
Implement backlog card BE-07 only after BE-05 is complete. A human selected BE-07. BE-06 may be in progress; you may seed RPMP tables in tests without a live Orchestrator.

Read docs/backlog/BE/BE-02.md, BE-04.md, BE-07.md, and ADR Decision 8. Load gateway-backend.

Point dashboard GET usecases at RPMP repo aggregates. Keep frozen JSON. Keep stub or fixtures for unit tests. Do not call Orchestrator from handlers. Do not edit frontend/.

Run go test ./... and go vet ./... from backend/. Stop after BE-07.
```
