---
id: FE-14
title: Use Cases list and detail from local API
status: complete
domain: frontend
blocked_by:
  - BE-09
  - FE-07
fr:
  - FR-3.1
  - FR-4.1
---

# FE-14: Use Cases list and detail from local API

Human-selected on 2026-09-11. Implemented on `feature/fe-14-use-cases-api`.

When `RPMP_DATA_MODE=api`, `/use-cases` and `/use-cases/[id]` call the local Go API with cookies. Do not keep feeding those routes from `mock-provider`.

Read `docs/plans/USE_CASES_API_2026-09-11.md` and paste live JSON into this card's Frozen contract if the examples in BE-09 drift after implementation. Prefer BE-09 as source of truth.

## In scope

- Typed fetch in `frontend/lib/api/` for `GET /api/v1/use-cases` and `GET /api/v1/use-cases/{id}`. JWT stays httpOnly.
- List: search box may stay client-side **or** pass `q` to the API. If you keep client-side search, still load the API list, not mock.
- Remove or hide Attended / Unattended tabs in api mode. CSV has no automation type. Optional: filter by `status` active/inactive if you add a control that matches BE-09.
- Columns that exist on the API: name, success rate (handle `null`), status `active`/`inactive`, failed count (issues column becomes failed/error+stopped, labeled honestly). Drop owner subtitle unless you show `environments` as muted text.
- Detail: identity, four KPIs from API counts (volume, success, failed, success rate). Process table from `processes`. Freshness sentence, not "live".
- Weekly chart and Issues ticket table: empty state that says this snapshot has no execution history. Do not pad with mock issues or fake trend points.
- `404` from API → existing not-found UI. Auth errors same pattern as Overview.
- Tests for list/detail in api mode. Report/email/settings still mock.

## Out of scope

- Backend files and new API fields.
- Orchestrator from the browser.
- Health, SLA, recent executions, report/email/settings live.
- Forcing mock `UseCase` (`Running`, `Error A`, `owner`) onto the API JSON.

## Frozen contract

BE-09 JSON. Do not invent keys. Map UI labels to those fields only.

Path: `GET /api/v1/use-cases` and `GET /api/v1/use-cases/{id}`

## Acceptance

- [x] With `RPMP_DATA_MODE=api`, both use-case routes show RPMP API data after a local sync, not `rpa-data` mock.
- [x] No invented owner, automation type, weekly series, or issue tickets.
- [x] Success rate null and empty list have a clear empty state.
- [x] JWT stays httpOnly.
- [x] `npm run typecheck`, `npm run lint`, and `npm test` pass from `frontend/`.
- [x] No `backend/` edits.

## Execution prompt

```text
Implement backlog card FE-14 only after BE-09 is complete. A human selected FE-14.

Read AGENTS.md, docs/adrs/ADR_FRONTEND.md, docs/plans/USE_CASES_API_2026-09-11.md, docs/backlog/BE/BE-09.md, and docs/backlog/FE/FE-14.md. Load gateway-frontend.

Point /use-cases and /use-cases/[id] at the local RPMP API when RPMP_DATA_MODE=api. Use frozen BE-09 JSON only. Hide mock-only tabs and charts. Do not invent fields. Do not edit backend/. Leave report, email, and settings on mock.

Run npm run typecheck, npm run lint, and npm test from frontend/. Stop after FE-14. Do not pick the next card.
```
