---
id: FE-01
title: Login page
status: blocked
domain: frontend
blocked_by:
  - BE-01
fr:
  - FR-1.1
  - FR-1.2
---

# FE-01: Login page

Blocked until BE-01 is complete and a human selects this card.

## In scope

- Create the minimum approved Next.js frontend scaffold if none exists.
- Add a login page for Employee ID and password.
- Submit to the RPMP login endpoint with cookies enabled.
- Show pending, invalid-credential, unavailable, and unexpected-error states without exposing technical detail.
- Redirect an authenticated user to Dashboard.
- Use `GET /api/v1/auth/me` to resolve the current user for route gating.
- Add focused component and route tests.

## Out of scope

- Any backend, database, JWT, cookie-issuing, or auth usecase work.
- SSO, password reset, remember-me, refresh tokens, and user administration.
- Dashboard KPI cards.
- Orchestrator calls, fields, credentials, or robot controls.

## Frozen contract

Submit:

```json
{
  "employee_id": "12345678",
  "password": "user supplied secret"
}
```

`POST /api/v1/auth/login` and `GET /api/v1/auth/me` return:

```json
{
  "user": {
    "id": "018f5f71-9cb9-7a61-97e7-d8f33f12b821",
    "employee_id": "12345678",
    "display_name": "Example Viewer",
    "role": "viewer"
  }
}
```

The browser receives `rpmp_access` as an httpOnly cookie. Frontend JavaScript must not read or store it. Client requests use credentials. Errors use:

```json
{
  "code": "invalid_credentials",
  "message": "Employee ID or password is incorrect."
}
```

## Acceptance

- [ ] The form accepts Employee ID and password with accessible labels.
- [ ] Submission uses the BE-01 endpoint and includes cookie credentials.
- [ ] A successful login redirects to Dashboard.
- [ ] `invalid_credentials` shows safe human-readable copy.
- [ ] Pending state prevents duplicate submissions.
- [ ] Password input and API errors are not logged or persisted.
- [ ] Frontend code never reads or stores the JWT.
- [ ] `me` gates protected routes without duplicating backend authorization.
- [ ] No backend files or contracts are changed.
- [ ] Frontend type, lint, and test commands pass.

## Execution prompt

```text
Implement backlog card FE-01 only after BE-01 is complete and a human selects FE-01.

Read AGENTS.md, docs/plans/VERTICAL_SLICE_2026-09-06.md, docs/adrs/ADR_FRONTEND.md, and PRD FR-1.1 and FR-1.2. Load gateway-frontend. Build the login page against the frozen BE-01 contract. Keep the JWT in the httpOnly cookie, use the RPMP API only, and make no backend changes. Run the frontend checks and obtain a frontend-reviewer verdict. Stop after FE-01. Do not select or start FE-02.
```
