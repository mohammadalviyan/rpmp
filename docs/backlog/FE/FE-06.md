---
id: FE-06
title: Port Overview page from bot-metrics-center
status: blocked
domain: frontend
blocked_by:
  - FE-05
fr:
  - FR-2.2
  - FR-2.3
  - FR-2.6
---

# FE-06: Port Overview page from bot-metrics-center

Copy `src/routes/index.tsx` (and `rpa-badges.tsx`) into the RPMP dashboard route. Keep six KPI cards, charts, automation type bar, and top use cases list. Data from mock provider in presentation mode.

## Source files to copy then adapt

- `src/routes/index.tsx`
- `src/components/rpa-badges.tsx`
- Recharts usage as in the source (AreaChart, PieChart, BarChart)

## In scope

- Overview at `/dashboard` (or `/` inside protected layout — match existing RPMP dashboard URL).
- All six sample KPIs in mock mode (including Unattended / Attended / Report Sent).
- Optional: if `RPMP_DATA_MODE=api`, map the five existing Go summary fields and hide mock-only cards rather than inventing API fields.
- “View all” links to `/use-cases` (page may land in FE-07).
- Tests for provider wiring, not for pixel-perfect SVG.

## Out of scope

- Use Cases list/detail bodies.
- Backend. New Go fields for attended/unattended.
- From-scratch chart redesign.

## Frozen contract

Mock: `summary`, `performanceSeries`, `errorDistribution`, `automationTypeSplit`, `useCases` from mock-provider (same shapes as `rpa-data.ts`).

API mode (optional): existing BE-02 / BE-04 JSON only.

## Acceptance

- [ ] Overview visually matches the sample layout (BRI colors).
- [ ] Numbers come from the provider, not hardcoded in the page (except hints that exist in the source).
- [ ] Demo banner still visible in mock mode.
- [ ] Frontend checks pass. No `backend/` edits.

## Execution prompt

```text
Implement backlog card FE-06 only after FE-05 is complete. A human selected FE-06.

Read docs/plans/PRESENTATION_UI_PORT_2026-09-07.md and docs/backlog/FE/FE-06.md. Load gateway-frontend.

Copy src/routes/index.tsx from /Users/alviyan/Project/reference/bot-metrics-center. Adapt to Next. Use mock-provider. Do not rebuild the charts from a blank file. BRI tokens. Do not edit backend/.

Run npm run typecheck, npm run lint, and npm test from frontend/. Stop after FE-06.
```
