---
id: FE-05
title: Port admin shell and mock data provider
status: ready
domain: frontend
blocked_by:
  - FE-03
fr:
  - UI-1
  - UI-2
---

# FE-05: Port admin shell and mock data provider

Foundation for the presentation UI. Copy shell and data from `bot-metrics-center`. Do not rewrite from scratch. Do not replace Next.js with TanStack Start.

Read `docs/plans/PRESENTATION_UI_PORT_2026-09-07.md` first.

## Source files to copy then adapt

From `/Users/alviyan/Project/reference/bot-metrics-center`:

- `src/components/app-sidebar.tsx`
- `src/components/dashboard-header.tsx`
- `src/components/theme-toggle.tsx`
- `src/components/ui/sidebar.tsx` (and any missing shadcn deps it imports)
- `src/lib/rpa-data.ts` → split into `frontend/lib/data/types.ts` + `frontend/lib/data/mock-provider.ts`
- `src/assets/rpa-logo.png` (or equivalent) if needed; rebrand alt text to RPMP

## In scope

- Protected layout: collapsible sidebar + header + outlet, same IA as the sample.
- Nav groups and hrefs for all sample menus (Overview, Use Cases, Generate Report, Report History, Email Management, Email History, General / Users / Email settings). Pages may 404 until later cards; links must exist.
- Branding: RPMP, BRI tokens, Inter. Active nav uses Cakrawala, not IRPAT red.
- `RpaDataProvider` interface + `mock-provider` + stub `api-provider`. Env `RPMP_DATA_MODE` default `mock`.
- Demo banner when mock mode is on.
- Logout still uses existing RPMP auth cookie. Header user from `GET /api/v1/auth/me` when logged in.
- Install `recharts` in `frontend/` if missing.

## Out of scope

- Building Overview charts, Use Cases table, report/email/settings page bodies (later cards).
- Backend edits. Real email. TanStack Start.
- Inventing a new layout that is “inspired by” but not copied from the source files.

## Frozen contract

No new Go JSON. Provider types must match `rpa-data.ts` exports (`UseCase`, `summary`, `performanceSeries`, etc.).

## Acceptance

- [ ] Files were ported from the source list, not authored as a blank shell.
- [ ] Sidebar matches sample grouping and collapse.
- [ ] BRI + Inter, not red IRPAT / Space Grotesk.
- [ ] Mock provider is the only place sample numbers live.
- [ ] JWT stays httpOnly.
- [ ] `npm run typecheck`, `npm run lint`, `npm test` pass.
- [ ] No `backend/` edits.

## Execution prompt

```text
Implement backlog card FE-05 only. A human selected this card.

Read docs/plans/PRESENTATION_UI_PORT_2026-09-07.md, AGENTS.md, docs/adrs/ADR_FRONTEND.md, docs/backlog/FE/FE-05.md, and docs/design/bri-tokens.css. Load gateway-frontend.

Copy the listed files from /Users/alviyan/Project/reference/bot-metrics-center into frontend/. Adapt TanStack Router to Next.js. Do not rewrite the sidebar/header from scratch. Do not clone the Start app over frontend/. Put rpa-data behind mock-provider. BRI + Inter. Demo banner in mock mode. Keep RPMP login cookies.

Run npm run typecheck, npm run lint, and npm test from frontend/. Stop after FE-05. Do not pick FE-06.
```
