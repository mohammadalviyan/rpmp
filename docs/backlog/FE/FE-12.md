---
id: FE-12
title: Overview live API and freshness copy
status: complete
domain: frontend
blocked_by:
  - BE-07
  - FE-06
fr:
  - FR-2.2
  - FR-2.3
  - FR-2.6
---

# FE-12: Overview live API and freshness copy

Wire Overview (and only Overview KPIs/charts that already have Go routes) to `api-provider` when `RPMP_DATA_MODE=api`. Show last refresh time. Do not say "live" or "real-time."

Other menus stay on mock-provider.

## In scope

- `RPMP_DATA_MODE=api` uses existing `/api/v1/dashboard/summary`, `execution-trend`, `errors` with cookies.
- Map five API KPIs; mock-only cards (Unattended / Attended / Report Sent) hidden or labeled Preview still from mock, **or** hidden in api mode. Do not invent API fields.
- Freshness sentence from `freshness.last_successful_refresh_at` / `status`.
- Keep login JWT httpOnly.

## Out of scope

- Sync button (FE-13). Use case list live. Backend. Calling Orchestrator from the browser.

## Frozen contract

BE-02 and BE-04 JSON. Optional GET sync/status if already deployed; not required for this card.

## Acceptance

- [x] Overview numbers in api mode come from Go, not rpa-data.
- [x] Copy states data is a RPMP copy, not a live tail.
- [x] Report/email/settings still mock.
- [x] Frontend checks pass. No `backend/` edits.

## Execution prompt

```text
Implement backlog card FE-12 only after BE-07 and FE-06 are complete. A human selected FE-12.

Read docs/plans/LOCAL_DB_SYNC_2026-09-09.md and docs/backlog/FE/FE-12.md. Load gateway-frontend.

Point Overview dashboard widgets at RPMP_DATA_MODE=api and existing dashboard JSON. Show freshness. Do not claim real-time. Leave other routes on mock. Do not add a sync button. Do not edit backend/.

Run npm run typecheck, npm run lint, and npm test from frontend/. Stop after FE-12.
```
