---
id: FE-07
title: Port Use Cases list and detail
status: complete
domain: frontend
blocked_by:
  - FE-05
fr:
  - FR-3.1
  - FR-4.1
  - FR-4.3
---

# FE-07: Port Use Cases list and detail

Human-selected on 2026-09-09. Implemented on `feature/fe-07-use-cases`.

Copy list + detail routes from the sample. Mock data. Presentation.

## Source files to copy then adapt

- `src/routes/use-cases.index.tsx`
- `src/routes/use-cases.$useCaseId.tsx`

## In scope

- `/use-cases` search, All / Unattended / Attended tabs, table with progress, status badges, issue count, type, row click.
- `/use-cases/[id]` back link, four KPIs, weekly chart, issues table.
- Data only from mock-provider `useCases` / `getUseCase`.

## Out of scope

- Go `GET /api/v1/use-cases` (no BE card yet). Do not invent live API calls.
- Report/email/settings pages.
- From-scratch table.

## Frozen contract

Mock `UseCase` and `Issue` types from FE-05 provider (same as `rpa-data.ts`).

## Acceptance

- [x] List and detail match sample interaction.
- [x] Unknown id shows a not-found state.
- [x] Frontend checks pass. No `backend/` edits.

## Execution prompt

```text
Implement backlog card FE-07 only after FE-05 is complete. A human selected FE-07.

Read docs/plans/PRESENTATION_UI_PORT_2026-09-07.md and docs/backlog/FE/FE-07.md. Load gateway-frontend.

Copy use-cases.index.tsx and use-cases.$useCaseId.tsx from /Users/alviyan/Project/reference/bot-metrics-center. Adapt to Next app router. Use mock-provider. Do not rewrite the list/detail from scratch. Do not call Go use-case APIs. Do not edit backend/.

Run npm run typecheck, npm run lint, and npm test from frontend/. Stop after FE-07.
```
